# AquaTransport header rewrites.
#
# One rule per block, blocks separated by blank lines:
#   1. scope        -- "*" or a space-separated list of app bundle names
#   2. URL pattern  -- prefix match; "*" is the only wildcard ("?" is literal)
#   3. one or more  -- "Name: value" headers to set on the request
#
# See redirects.txt for the note on which process scope actually matches.

# Dictionary's Wikipedia panel
Dictionary
https://*.wikipedia.org/w/api.php?action=
User-Agent: Legacy Mac OS X via Aqua Proxy (https://mavericksforever.com)
